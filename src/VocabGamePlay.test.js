// test that each method function within component functions
// make sure to test whether updating gameDataset in function 
// (through initial state assignment) and methods changes the global dataset.